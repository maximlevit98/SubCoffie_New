# 💾 Как сохранить изменения в Git - Полное руководство

## 🚀 САМЫЙ ПРОСТОЙ СПОСОБ (Используйте скрипт)

```bash
./quick-save.sh
```

Скрипт сделает все автоматически:
1. Покажет что изменилось
2. Спросит описание изменений
3. Сохранит локально
4. Отправит на GitHub

---

## 📝 РУЧНОЙ СПОСОБ (3 команды)

### Базовый процесс:

```bash
# 1️⃣ Добавить ВСЕ изменения
git add .

# 2️⃣ Сохранить с комментарием
git commit -m "Описание изменений"

# 3️⃣ Отправить на GitHub
git push origin main
```

### Пример:

```bash
git add .
git commit -m "feat: добавил функцию заказа кофе"
git push origin main
```

---

## 🎯 ПОШАГОВАЯ ИНСТРУКЦИЯ

### Шаг 1: Проверьте изменения

```bash
git status
```

Вы увидите:
- **Красным** - измененные файлы (еще не добавлены)
- **Зеленым** - добавленные файлы (готовы к commit)

### Шаг 2: Посмотрите ЧТО изменилось (опционально)

```bash
git diff
```

Покажет детальные изменения в файлах

### Шаг 3: Добавьте файлы

**Вариант A - Добавить ВСЕ файлы:**
```bash
git add .
```

**Вариант B - Добавить КОНКРЕТНЫЕ файлы:**
```bash
git add путь/к/файлу.swift
git add путь/к/другому/файлу.ts
```

### Шаг 4: Создайте коммит

```bash
git commit -m "Ваше описание изменений"
```

### Шаг 5: Отправьте на GitHub

```bash
git push origin main
```

---

## 📋 ПРАВИЛА ДЛЯ COMMIT MESSAGES

### Формат: `type: описание`

**Types (типы изменений):**

- `feat:` - новая функция
  ```bash
  git commit -m "feat: добавил страницу заказа кофе"
  ```

- `fix:` - исправление бага
  ```bash
  git commit -m "fix: исправил ошибку при оплате"
  ```

- `docs:` - документация
  ```bash
  git commit -m "docs: обновил README"
  ```

- `style:` - форматирование (не влияет на код)
  ```bash
  git commit -m "style: исправил отступы в коде"
  ```

- `refactor:` - рефакторинг кода
  ```bash
  git commit -m "refactor: улучшил структуру OrderService"
  ```

- `test:` - добавление тестов
  ```bash
  git commit -m "test: добавил тесты для авторизации"
  ```

- `chore:` - обслуживание (зависимости и т.д.)
  ```bash
  git commit -m "chore: обновил npm пакеты"
  ```

---

## 💡 ЧАСТЫЕ СЦЕНАРИИ

### Сценарий 1: Сохранить ВСЕ изменения

```bash
git add .
git commit -m "feat: добавил новые функции"
git push origin main
```

### Сценарий 2: Сохранить только НЕКОТОРЫЕ файлы

```bash
# Backend изменения
git add SubscribeCoffieBackend/
git commit -m "feat(backend): добавил новый API endpoint"
git push origin main

# iOS изменения
git add SubscribeCoffieClean/
git commit -m "feat(ios): добавил экран профиля"
git push origin main
```

### Сценарий 3: Отменить добавление файла (до commit)

```bash
# Убрать файл из staging
git restore --staged имя_файла.swift

# ИЛИ убрать ВСЕ файлы
git restore --staged .
```

### Сценарий 4: Отменить последний коммит (до push)

```bash
# Отменить commit, но СОХРАНИТЬ изменения
git reset --soft HEAD~1

# Отменить commit и УДАЛИТЬ изменения (осторожно!)
git reset --hard HEAD~1
```

### Сценарий 5: Изменить последний commit message

```bash
git commit --amend -m "Новое описание"
```

---

## 🔄 РАБОТА С ВЕТКАМИ

### Создать новую ветку для функции:

```bash
# 1. Создать и переключиться на новую ветку
git checkout -b feature/my-new-feature

# 2. Внести изменения и сохранить
git add .
git commit -m "feat: добавил новую функцию"

# 3. Отправить ветку на GitHub
git push -u origin feature/my-new-feature

# 4. На GitHub создайте Pull Request
```

### Вернуться на main:

```bash
git checkout main
```

---

## 🆘 РЕШЕНИЕ ПРОБЛЕМ

### Проблема: "Changes not staged for commit"

**Решение:**
```bash
git add .
```

### Проблема: "Your branch is ahead of 'origin/main'"

**Решение:**
```bash
git push origin main
```

### Проблема: "Your branch is behind 'origin/main'"

**Решение:**
```bash
git pull origin main
```

### Проблема: Случайно добавил .env файл

**Решение:**
```bash
# Убрать из tracking
git rm --cached .env

# Убедитесь что .env в .gitignore
echo ".env" >> .gitignore

# Сохраните изменения
git add .gitignore
git commit -m "chore: remove .env from tracking"
git push origin main
```

### Проблема: Конфликт при push

**Решение:**
```bash
# 1. Получите последние изменения
git pull origin main

# 2. Если есть конфликты - откройте файлы и разрешите их
# 3. После разрешения конфликтов:
git add .
git commit -m "merge: resolve conflicts"
git push origin main
```

---

## 📊 ПОЛЕЗНЫЕ КОМАНДЫ

### Просмотр:

```bash
git status              # Текущее состояние
git log --oneline       # История коммитов
git log -3              # Последние 3 коммита
git diff                # Что изменилось
git remote -v           # Список remote репозиториев
```

### История изменений:

```bash
# Красивая история
git log --oneline --graph --all --decorate

# Кто изменил файл
git blame имя_файла.swift

# Найти когда был введен баг
git bisect start
```

---

## 🎯 ЕЖЕДНЕВНЫЙ WORKFLOW

### Начало работы:

```bash
# 1. Обновите код
git pull origin main

# 2. Создайте ветку для задачи
git checkout -b feature/task-name
```

### В процессе работы:

```bash
# Часто сохраняйте локально
git add .
git commit -m "feat: work in progress"
```

### Завершение работы:

```bash
# 1. Финальный коммит
git add .
git commit -m "feat: completed feature X"

# 2. Отправьте на GitHub
git push origin feature/task-name

# 3. Создайте Pull Request на GitHub
```

---

## 🔐 БЕЗОПАСНОСТЬ

### ⚠️ НИКОГДА не коммитьте:

- ❌ `.env` файлы (пароли, ключи API)
- ❌ `node_modules/` (зависимости)
- ❌ Файлы с паролями
- ❌ API ключи и токены
- ❌ Личные данные пользователей

### ✅ ВСЕГДА проверяйте перед push:

```bash
# Посмотрите что добавляете
git status
git diff --staged

# Убедитесь что .env не добавлен
git ls-files | grep .env
```

---

## 📚 ССЫЛКИ НА ДОКУМЕНТАЦИЮ

**В вашем проекте:**
- `./quick-save.sh` - Быстрое сохранение
- `./git-quick-reference.sh` - Полная справка по Git
- `GITHUB_SETUP.md` - Настройка GitHub
- `CONTRIBUTING.md` - Правила контрибьюции

**Ваш репозиторий:**
- https://github.com/maximlevit98/SubCoffie_New

**Внешние ресурсы:**
- https://git-scm.com/doc
- https://www.conventionalcommits.org

---

## 💪 ПРАКТИКА

### Упражнение 1: Простое сохранение

1. Измените любой файл
2. `git status` - посмотрите изменения
3. `git add .` - добавьте файлы
4. `git commit -m "test: мой первый коммит"` - сохраните
5. `git push origin main` - отправьте на GitHub

### Упражнение 2: Используйте скрипт

1. Измените файл
2. `./quick-save.sh` - запустите скрипт
3. Следуйте инструкциям

---

## ✨ СОВЕТЫ ПРОФИ

1. **Коммитьте часто** - маленькие коммиты лучше больших
2. **Пишите понятные сообщения** - будущий вы скажет спасибо
3. **Pull перед push** - избегайте конфликтов
4. **Используйте ветки** - не работайте напрямую в main
5. **Проверяйте перед push** - `git status` и `git diff`

---

Made with ☕ and ❤️
