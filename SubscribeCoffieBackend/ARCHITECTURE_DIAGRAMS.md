# Owner Admin Panel - Architecture Diagrams

## 🏗️ System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                        Owner Admin Panel                         │
│                                                                   │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                    Frontend (Next.js)                      │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │  │
│  │  │Dashboard │  │  Cafes   │  │  Menu    │  │  Orders  │  │  │
│  │  │   Page   │  │   List   │  │Management│  │  Kanban  │  │  │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘  │  │
│  └───────────────────────────────────────────────────────────┘  │
│                              ↕                                    │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │              Backend API (Supabase)                        │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │  │
│  │  │   REST   │  │   RPC    │  │Real-time │  │   Auth   │  │  │
│  │  │   API    │  │Functions │  │WebSocket │  │  & RLS   │  │  │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘  │  │
│  └───────────────────────────────────────────────────────────┘  │
│                              ↕                                    │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │            Database (PostgreSQL)                           │  │
│  │  ┌──────────────────────────────────────────────────────┐ │  │
│  │  │ accounts │ cafes │ menu_* │ orders │ history │ ...   │ │  │
│  │  └──────────────────────────────────────────────────────┘ │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

## 📊 Data Model

```
┌────────────────┐
│    profiles    │
│  (auth.users)  │
└────────┬───────┘
         │ owner_user_id
         │
         ▼
┌────────────────┐
│    accounts    │◄─── One account per owner
│   (org level)  │
└────────┬───────┘
         │ account_id (1:N)
         │
         ▼
┌────────────────┐
│     cafes      │◄─── Multiple cafes per account
│   (locations)  │
└────┬───────────┘
     │
     ├───────────────────────────┐
     │                           │
     ▼                           ▼
┌────────────────┐    ┌────────────────┐
│menu_categories │    │     orders     │
└────────┬───────┘    └────────┬───────┘
         │                     │
         ▼                     ▼
┌────────────────┐    ┌────────────────┐
│  menu_items    │    │  order_items   │
└────────┬───────┘    └────────────────┘
         │
         ▼
┌────────────────┐
│menu_modifiers  │
└────────────────┘
```

## 🔐 Access Control Matrix

```
┌──────────────────┬─────────┬─────────┬──────────┬───────┐
│      Table       │  Public │  Owner  │ Customer │ Admin │
├──────────────────┼─────────┼─────────┼──────────┼───────┤
│ accounts         │    ❌   │   ✅    │    ❌    │   ✅  │
│ cafes            │   📖*   │   ✅    │    ❌    │   ✅  │
│ menu_categories  │   📖*   │   ✅    │    ❌    │   ✅  │
│ menu_items       │   📖*   │   ✅    │    ❌    │   ✅  │
│ menu_modifiers   │   📖*   │   ✅    │    ❌    │   ✅  │
│ orders           │    ❌   │   📖**  │   ✅***  │   ✅  │
│ publication_hist │    ❌   │   📖    │    ❌    │   ✅  │
└──────────────────┴─────────┴─────────┴──────────┴───────┘

Legend:
✅ = Full access (CRUD)
📖 = Read only
❌ = No access
* = Only for published cafes
** = Only for their cafe's orders
*** = Only their own orders
```

## 🔄 Order Status Flow

```
          Customer Creates Order
                   │
                   ▼
            ┌─────────────┐
            │   CREATED   │◄───── New order arrives
            └──────┬──────┘
                   │
         Owner accepts │ rejects/cancels
                   ▼
            ┌─────────────┐
            │  ACCEPTED   │◄───── Owner confirmed
            └──────┬──────┘
                   │
           Owner starts preparing
                   ▼
            ┌─────────────┐
            │ IN PROGRESS │◄───── Being prepared
            └──────┬──────┘
                   │
          Owner marks ready │ cancels
                   ▼
            ┌─────────────┐
            │    READY    │◄───── Notify customer
            └──────┬──────┘
                   │
        Customer picks up │ no-show
                   ▼
            ┌─────────────┐
            │  PICKED UP  │◄───── Complete ✅
            └─────────────┘

        Alternative outcomes:
        ┌─────────────┐
        │  CANCELED   │◄───── Refund processed
        └─────────────┘
        
        ┌─────────────┐
        │   NO-SHOW   │◄───── Customer didn't arrive
        └─────────────┘
```

## 🚦 Cafe Status Flow

```
         Owner creates cafe
                │
                ▼
         ┌─────────────┐
         │    DRAFT    │◄───── Setup phase
         └──────┬──────┘       (add menu, details)
                │
      Submit for moderation
                │
                ▼
         ┌─────────────┐
         │ MODERATION  │◄───── Admin reviews
         └──────┬──────┘
                │
        ┌───────┴───────┐
        │               │
    Approve         Reject
        │               │
        ▼               ▼
  ┌─────────────┐ ┌─────────────┐
  │  PUBLISHED  │ │  REJECTED   │
  └──────┬──────┘ └──────┬──────┘
         │               │
    Owner pauses    Fix issues
         │               │
         ▼               ▼
  ┌─────────────┐ ┌─────────────┐
  │   PAUSED    │ │    DRAFT    │
  └──────┬──────┘ └─────────────┘
         │
    Owner resumes
         │
         ▼
  ┌─────────────┐
  │  PUBLISHED  │
  └─────────────┘
```

## 📋 Publication Checklist

```
┌─────────────────────────────────────────────────────────┐
│           Cafe Publication Readiness                     │
├─────────────────────────────────────────────────────────┤
│                                                           │
│  ☐ Basic Info (name, address, phone)        Required    │
│  ☐ Working Hours (schedule)                 Required    │
│  ☐ Storefront (logo, cover, description)    Required    │
│  ☐ Menu (at least 3 active items)           Required    │
│  ☐ Legal Data (INN in account)              Required    │
│  ☐ Coordinates (latitude, longitude)        Required    │
│                                                           │
│  Progress: ████████░░ 80%                                │
│                                                           │
│  ✅ = Complete    ⚠️ = Warning    ❌ = Required          │
│                                                           │
│  [Complete Setup]  [Submit for Review]                   │
└─────────────────────────────────────────────────────────┘
```

## 🎯 API Function Map

```
Account Level
├── get_or_create_owner_account()     Create/get account
├── get_owner_cafes()                 List all cafes
└── get_account_dashboard_stats()     Aggregate metrics

Cafe Level
├── duplicate_cafe()                   Clone cafe + menu
├── get_cafe_publication_checklist()   Readiness check
├── submit_cafe_for_moderation()       Submit workflow
├── get_cafe_dashboard_stats()         Cafe metrics
└── get_cafe_orders_by_status()        Kanban data

Order Management
├── owner_update_order_status()        Change status
├── owner_cancel_order()               Cancel + refund
├── get_cafe_orders()                  Filtered list
└── get_order_details()                Full details

Menu Management
└── toggle_menu_item_stop_list()       Availability
```

## 🔌 Real-time Architecture

```
┌───────────────────────────────────────────────────────┐
│                    Frontend Client                     │
│  ┌─────────────────────────────────────────────────┐  │
│  │         Supabase.js Client                      │  │
│  │  ┌──────────────────────────────────────────┐  │  │
│  │  │   channel.subscribe('orders')            │  │  │
│  │  │   .on('INSERT', callback)                │  │  │
│  │  └──────────────┬───────────────────────────┘  │  │
│  └─────────────────┼───────────────────────────────┘  │
└────────────────────┼───────────────────────────────────┘
                     │
                     ▼ WebSocket
┌────────────────────────────────────────────────────────┐
│              Supabase Realtime Server                  │
│  ┌──────────────────────────────────────────────────┐ │
│  │  postgres_changes                                │ │
│  │  schema: public, table: orders                   │ │
│  │  filter: cafe_id=eq.uuid                         │ │
│  └──────────────┬───────────────────────────────────┘ │
└─────────────────┼──────────────────────────────────────┘
                  │
                  ▼ Listen to DB changes
┌────────────────────────────────────────────────────────┐
│              PostgreSQL Database                       │
│  ┌──────────────────────────────────────────────────┐ │
│  │  orders table                                    │ │
│  │  - INSERT: New order created                     │ │
│  │  - UPDATE: Status changed                        │ │
│  │  - DELETE: Order removed                         │ │
│  └──────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────┘
```

## 🗂️ Menu Structure Example

```
Cafe: "Downtown Coffee"
│
├── Category: "Coffee" (sort_order: 0)
│   ├── Item: "Cappuccino" ($2.50)
│   │   ├── Modifier Group: "Volume" (required)
│   │   │   ├── Small (+$0)
│   │   │   ├── Medium (+$0.30)
│   │   │   └── Large (+$0.50)
│   │   └── Modifier Group: "Milk Type" (optional)
│   │       ├── Regular (+$0)
│   │       ├── Oat (+$0.40)
│   │       └── Almond (+$0.40)
│   │
│   ├── Item: "Espresso" ($1.50)
│   │   └── Modifier Group: "Shot" (required)
│   │       ├── Single (+$0)
│   │       └── Double (+$0.50)
│   │
│   └── Item: "Latte" ($2.80)
│       └── [Similar modifiers...]
│
├── Category: "Tea" (sort_order: 1)
│   └── [Tea items...]
│
└── Category: "Food" (sort_order: 2)
    └── [Food items...]
```

## 📱 Frontend Route Structure (Planned)

```
/admin/owner/
│
├── dashboard/                    ← Account Dashboard
│
├── cafes/
│   ├── index                     ← List of cafes
│   ├── new/                      ← Create cafe wizard
│   └── [id]/                     ← Edit cafe
│
├── cafe/[cafeId]/                ← Cafe-specific routes
│   ├── dashboard/                ← Cafe dashboard
│   ├── orders/                   ← Kanban board
│   │   └── [orderId]/            ← Order details
│   ├── menu/                     ← Menu management
│   │   ├── new/                  ← Add item
│   │   └── [itemId]/             ← Edit item
│   ├── settings/                 ← Cafe settings
│   ├── storefront/               ← Visual customization
│   ├── finances/                 ← Cafe financials
│   └── publication/              ← Checklist + submit
│
├── finances/                     ← Account finances
├── settings/                     ← Account settings
└── notifications/                ← Notifications inbox
```

## 🔄 Integration Flow Example

```
┌──────────────────────────────────────────────────────────┐
│  Step 1: User Signs Up                                   │
│  ────────────────────────────────────────────────────    │
│  supabase.auth.signUp()                                  │
│         │                                                 │
│         ▼                                                 │
│  profiles.role = 'owner'                                 │
└──────────────────────────────────────────────────────────┘
                       ▼
┌──────────────────────────────────────────────────────────┐
│  Step 2: Create Account                                  │
│  ────────────────────────────────────────────────────    │
│  get_or_create_owner_account(user_id, company_name)     │
│         │                                                 │
│         ▼                                                 │
│  accounts table: New record                              │
└──────────────────────────────────────────────────────────┘
                       ▼
┌──────────────────────────────────────────────────────────┐
│  Step 3: Create First Cafe                               │
│  ────────────────────────────────────────────────────    │
│  Insert into cafes (account_id, name, address...)       │
│         │                                                 │
│         ▼                                                 │
│  cafes table: status='draft'                            │
└──────────────────────────────────────────────────────────┘
                       ▼
┌──────────────────────────────────────────────────────────┐
│  Step 4: Add Menu                                        │
│  ────────────────────────────────────────────────────    │
│  Insert into menu_categories                             │
│  Insert into menu_items                                  │
│  Insert into menu_modifiers                              │
└──────────────────────────────────────────────────────────┘
                       ▼
┌──────────────────────────────────────────────────────────┐
│  Step 5: Submit for Review                               │
│  ────────────────────────────────────────────────────    │
│  submit_cafe_for_moderation(cafe_id)                     │
│         │                                                 │
│         ▼                                                 │
│  cafes.status = 'moderation'                            │
│  cafe_publication_history: New record                    │
└──────────────────────────────────────────────────────────┘
                       ▼
┌──────────────────────────────────────────────────────────┐
│  Step 6: Admin Approves                                  │
│  ────────────────────────────────────────────────────    │
│  cafes.status = 'published'                             │
│         │                                                 │
│         ▼                                                 │
│  Cafe visible to public ✅                               │
└──────────────────────────────────────────────────────────┘
```

## 💾 Database Performance

```
Table Indexes:
├── accounts
│   └── owner_user_id (UNIQUE)
├── cafes
│   ├── account_id
│   ├── status
│   └── (status, created_at)
├── menu_categories
│   ├── cafe_id
│   └── (cafe_id, sort_order)
├── menu_items
│   ├── category_id
│   └── (cafe_id, category, sort_order)
├── menu_modifiers
│   ├── menu_item_id
│   └── (menu_item_id, group_name, sort_order)
└── orders
    ├── cafe_id
    ├── user_id
    ├── order_type
    ├── payment_status
    └── slot_time (partial)
```

---

## 📝 Notes

- All diagrams represent the implemented backend structure
- Frontend routes shown are planned (not yet implemented)
- Real-time subscriptions are configured and ready to use
- RLS policies enforce all access control rules
- All functions validate ownership before operations

---

**For detailed API usage, see [OWNER_API_CONTRACT.md](./OWNER_API_CONTRACT.md)**
