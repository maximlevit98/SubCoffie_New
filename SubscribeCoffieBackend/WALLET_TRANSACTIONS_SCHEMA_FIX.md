# Wallet Transactions Schema Alignment

## 📋 Задача
Привести структуру таблицы `public.wallet_transactions` в соответствие с RPC функциями из `wallet_sync_functions.sql`.

## 🔍 Выявленное несоответствие

### До исправления:

**Таблица** (`20260123150000_wallet_transactions.sql`):
```sql
create table public.wallet_transactions (
  id uuid,
  wallet_id uuid,
  direction text,  -- ❌ credit/debit
  amount int,
  reason text,     -- ❌ простое описание
  created_at timestamptz
);
```

**RPC функции** (`20260131010000_wallet_sync_functions.sql`):
```sql
insert into public.wallet_transactions (
  wallet_id,
  amount,
  type,              -- ✅ topup/bonus/payment/refund/admin_credit/admin_debit
  description,       -- ✅ детальное описание
  order_id,          -- ✅ связь с заказом
  actor_user_id,     -- ✅ кто выполнил операцию
  balance_before,    -- ✅ баланс до операции (аудит)
  balance_after      -- ✅ баланс после операции (аудит)
)
```

**Проблема**: RPC функции не могли работать с таблицей, так как пытались вставить в несуществующие колонки.

## ✅ Решение: Расширение таблицы

Выбран подход **расширения таблицы** вместо упрощения RPC, так как:

1. **Аудит**: `balance_before`/`balance_after` критичны для финансового аудита
2. **Детализация**: `type` (6 типов) более информативен чем `direction` (2 значения)
3. **Связи**: `order_id` нужен для привязки транзакций к заказам
4. **Прозрачность**: `actor_user_id` важен для отслеживания админских операций

## 📦 Внесённые изменения

### 1. Обновлена исходная миграция

**Файл**: `supabase/migrations/20260123150000_wallet_transactions.sql`

**Новая структура**:
```sql
create table public.wallet_transactions (
  id uuid primary key,
  wallet_id uuid not null references public.wallets(id) on delete cascade,
  amount int not null,
  type text not null,                -- topup, bonus, payment, refund, admin_credit, admin_debit
  description text,                  -- Детальное описание операции
  order_id uuid references public.orders(id) on delete set null,  -- Связь с заказом
  actor_user_id uuid references auth.users(id) on delete set null,  -- Кто выполнил
  balance_before int not null,       -- Баланс до операции
  balance_after int not null,        -- Баланс после операции
  created_at timestamptz not null default now(),
  constraint wallet_transactions_type_check 
    check (type in ('topup', 'bonus', 'payment', 'refund', 'admin_credit', 'admin_debit'))
);
```

**Индексы**:
```sql
- wallet_id_idx (для быстрого поиска транзакций кошелька)
- created_at_idx (для хронологической сортировки)
- type_idx (для фильтрации по типу)
- order_id_idx (для поиска транзакций заказа)
- actor_user_id_idx (для поиска действий админа)
```

### 2. Создана миграция для существующих БД

**Файл**: `supabase/migrations/20260205000002_expand_wallet_transactions.sql`

**Операции**:
1. Добавление новых колонок (`type`, `description`, `order_id`, `actor_user_id`, `balance_before/after`)
2. Миграция данных: `direction` → `type`, `reason` → `description`
3. Удаление старых колонок
4. Добавление NOT NULL constraints
5. Добавление check constraint для `type`
6. Создание индексов

### 3. Обновлены тесты

**Файл**: `tests/wallets_rpc.test.sql`

**Новые тесты**:

#### Test 1.2.7 (расширен)
- ✅ Проверка наличия `balance_before/after` в транзакциях
- ✅ Проверка корректности расчета `balance_after` для разных типов:
  - `topup/bonus/refund/admin_credit`: `balance_after = balance_before + amount`
  - `payment/admin_debit`: `balance_after = balance_before - abs(amount)`

#### Test 1.2.7b (новый)
- ✅ Проверка сохранения `order_id` и `actor_user_id`
- ✅ Тест admin операций с полным аудитом

## 📊 Типы транзакций

| Type | Direction | Description | Example |
|------|-----------|-------------|---------|
| `topup` | Credit | Пополнение баланса через платежную систему | Пополнение на 1000₽ |
| `bonus` | Credit | Начисление бонусов | Бонус за регистрацию 50₽ |
| `payment` | Debit | Оплата заказа | Оплата заказа #123 |
| `refund` | Credit | Возврат средств | Отмена заказа #123 |
| `admin_credit` | Credit | Ручное начисление админом | Компенсация клиенту |
| `admin_debit` | Debit | Ручное списание админом | Корректировка баланса |

## 🔍 Audit Trail Example

Каждая транзакция теперь содержит полный audit trail:

```sql
INSERT INTO wallet_transactions VALUES (
  id: 'uuid-123',
  wallet_id: 'wallet-uuid',
  amount: 500,
  type: 'topup',
  description: 'Пополнение картой *1234',
  order_id: null,
  actor_user_id: null,  -- Сам пользователь
  balance_before: 1200,  -- ✅ Было 1200₽
  balance_after: 1700,   -- ✅ Стало 1700₽
  created_at: now()
);
```

Admin операция:
```sql
INSERT INTO wallet_transactions VALUES (
  id: 'uuid-456',
  wallet_id: 'wallet-uuid',
  amount: 100,
  type: 'admin_credit',
  description: 'Компенсация за задержку доставки',
  order_id: 'order-uuid',
  actor_user_id: 'admin-uuid',  -- ✅ ID администратора
  balance_before: 1700,
  balance_after: 1800,
  created_at: now()
);
```

## ✅ Преимущества новой структуры

### 1. Полный аудит
- ✅ `balance_before/after` позволяют проверить любую транзакцию
- ✅ Можно пересчитать баланс из истории транзакций
- ✅ Защита от несанкционированных изменений

### 2. Детальная типизация
```
Было:  credit | debit
Стало: topup | bonus | payment | refund | admin_credit | admin_debit
```
- ✅ Понятно откуда пришли деньги
- ✅ Понятно куда ушли деньги
- ✅ Различаются бонусы и реальные деньги

### 3. Связи
- ✅ `order_id` → можно получить все транзакции заказа
- ✅ `actor_user_id` → можно отследить действия конкретного админа
- ✅ Полная прослеживаемость операций

### 4. Защита от фрода
- ✅ Невозможно изменить баланс без создания транзакции
- ✅ Все изменения фиксируются с контекстом
- ✅ Админские операции имеют автора

## 🧪 Тестирование

### Запуск тестов

```bash
cd SubscribeCoffieBackend
psql $DATABASE_URL -f tests/wallets_rpc.test.sql
```

### Ожидаемый вывод

```
✅ PASS: Кошелек получен, balance = 500
✅ PASS: Все поля присутствуют
✅ PASS: Новый кошелек создан с нулевым балансом
✅ PASS: Баланс увеличен на 200
✅ PASS: Бонусный баланс увеличен
✅ PASS: Баланс уменьшен на 100
✅ PASS: Корректная ошибка для невалидного типа
✅ PASS: Получено N транзакций
✅ PASS: Поля balance_before/after присутствуют
✅ PASS: balance_after корректно рассчитан
✅ PASS: order_id и actor_user_id корректно сохранены
✅ PASS: Статистика получена
✅ PASS: Все поля статистики присутствуют
```

## 📋 Deployment

### Development / Staging

```bash
cd SubscribeCoffieBackend

# Полный сброс (применит все миграции)
supabase db reset

# Проверка
psql $DATABASE_URL -c "\d public.wallet_transactions"
```

### Production (только для существующих БД)

```bash
cd SubscribeCoffieBackend

# Применить только новую миграцию
supabase db push

# Проверка
psql $PRODUCTION_DB_URL -c "
  SELECT column_name, data_type, is_nullable 
  FROM information_schema.columns 
  WHERE table_name = 'wallet_transactions' 
  ORDER BY ordinal_position;
"
```

## 🔗 Связанные файлы

1. ✅ `supabase/migrations/20260123150000_wallet_transactions.sql` - обновлена структура
2. ✅ `supabase/migrations/20260205000002_expand_wallet_transactions.sql` - миграция для существующих БД
3. ✅ `supabase/migrations/20260131010000_wallet_sync_functions.sql` - теперь совместима с таблицей
4. ✅ `tests/wallets_rpc.test.sql` - добавлены тесты для новых полей

## 📝 Checklist

- [x] Обновлена структура таблицы wallet_transactions
- [x] Создана миграция для существующих БД
- [x] Добавлены индексы для новых колонок
- [x] Добавлены check constraints
- [x] Обновлены тесты
- [x] Добавлены комментарии к таблице и колонкам
- [x] Документация создана

## ⚠️ Breaking Changes

**Для существующих данных**: Миграция `20260205000002` автоматически конвертирует:
- `direction = 'credit'` → `type = 'topup'`
- `direction = 'debit'` → `type = 'payment'`
- `reason` → `description`

**Внимание**: После применения миграции колонки `direction` и `reason` будут удалены!

## 🚀 Next Steps

1. **Review** - проверить изменения
2. **Deploy** - применить миграции к базам
3. **Test** - запустить `wallets_rpc.test.sql`
4. **Monitor** - проверить что RPC функции работают корректно
5. **Audit** - убедиться что все транзакции содержат `balance_before/after`

---

**Status**: ✅ **COMPLETE** - Schema aligned, tests updated, ready for deployment  
**Date**: 2026-02-05
