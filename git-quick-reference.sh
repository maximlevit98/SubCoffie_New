#!/bin/bash

# 🚀 QUICK REFERENCE - Subscribe Coffee Git/GitHub
# Быстрая шпаргалка по командам

set -e

cat << 'EOF'
╔══════════════════════════════════════════════════════════════╗
║         Subscribe Coffee - Git Quick Reference               ║
╚══════════════════════════════════════════════════════════════╝

📁 ТЕКУЩЕЕ СОСТОЯНИЕ
──────────────────────────────────────────────────────────────
✅ Git репозиторий инициализирован
✅ 4 коммита созданы
✅ 507 файлов в репозитории
✅ Размер: ~781 MB
✅ Все готово к push на GitHub

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🚀 БЫСТРЫЙ СТАРТ

1. Создайте репозиторий на GitHub:
   https://github.com/new

2. Подключите и отправьте код:
   
   ./setup-github.sh
   
   ИЛИ вручную:
   
   git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git
   git push -u origin main

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📚 ОСНОВНЫЕ КОМАНДЫ

┌──────────────────────────────────────────────────────────────┐
│ Просмотр состояния                                           │
└──────────────────────────────────────────────────────────────┘
  git status                    # Текущее состояние
  git log --oneline            # История коммитов
  git log --graph --all        # Визуализация веток
  git remote -v                # Список remote репозиториев
  git branch -a                # Все ветки

┌──────────────────────────────────────────────────────────────┐
│ Работа с изменениями                                         │
└──────────────────────────────────────────────────────────────┘
  git add .                    # Добавить все изменения
  git add file.txt             # Добавить конкретный файл
  git commit -m "message"      # Закоммитить изменения
  git diff                     # Посмотреть изменения
  git diff --staged            # Посмотреть staged изменения

┌──────────────────────────────────────────────────────────────┐
│ Работа с ветками                                             │
└──────────────────────────────────────────────────────────────┘
  git checkout -b new-branch   # Создать и переключиться
  git checkout branch-name     # Переключиться на ветку
  git branch -d branch-name    # Удалить ветку
  git merge branch-name        # Слить ветку в текущую

┌──────────────────────────────────────────────────────────────┐
│ Работа с remote                                              │
└──────────────────────────────────────────────────────────────┘
  git push origin main         # Отправить на GitHub
  git push -u origin branch    # Отправить новую ветку
  git pull origin main         # Получить изменения
  git fetch origin             # Скачать без merge

┌──────────────────────────────────────────────────────────────┐
│ Отмена изменений                                             │
└──────────────────────────────────────────────────────────────┘
  git restore file.txt         # Отменить изменения в файле
  git restore --staged file    # Убрать из staging
  git reset --soft HEAD~1      # Отменить последний коммит
  git revert commit-hash       # Создать коммит отмены

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📋 CONVENTIONAL COMMITS

Формат: type(scope): subject

Types:
  feat:     Новая функция
  fix:      Исправление бага
  docs:     Документация
  style:    Форматирование
  refactor: Рефакторинг
  test:     Тесты
  chore:    Обслуживание

Примеры:
  git commit -m "feat(ios): Add subscription view"
  git commit -m "fix(backend): Fix order validation"
  git commit -m "docs: Update README"

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔧 ПОЛЕЗНЫЕ АЛИАСЫ (добавьте в ~/.gitconfig)

[alias]
  st = status
  co = checkout
  br = branch
  ci = commit
  lg = log --oneline --graph --all --decorate
  last = log -1 HEAD
  unstage = restore --staged

Использование:
  git st            # вместо git status
  git lg            # красивый лог
  git last          # последний коммит

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🌿 WORKFLOW ДЛЯ НОВОЙ ФУНКЦИИ

1. Создать ветку:
   git checkout -b feature/my-feature

2. Внести изменения и коммитить:
   git add .
   git commit -m "feat: add amazing feature"

3. Отправить на GitHub:
   git push -u origin feature/my-feature

4. Создать Pull Request на GitHub

5. После merge обновить main:
   git checkout main
   git pull origin main

6. Удалить локальную ветку:
   git branch -d feature/my-feature

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🆘 РЕШЕНИЕ ПРОБЛЕМ

┌──────────────────────────────────────────────────────────────┐
│ Проблема: Changes not staged                                 │
└──────────────────────────────────────────────────────────────┘
  Решение: git add .

┌──────────────────────────────────────────────────────────────┐
│ Проблема: Authentication failed (HTTPS)                      │
└──────────────────────────────────────────────────────────────┘
  Решение: Используйте Personal Access Token вместо пароля
  GitHub → Settings → Developer settings → Personal access tokens

┌──────────────────────────────────────────────────────────────┐
│ Проблема: SSH authentication failed                          │
└──────────────────────────────────────────────────────────────┘
  Решение: 
  ssh-keygen -t ed25519 -C "your_email@example.com"
  pbcopy < ~/.ssh/id_ed25519.pub
  # Добавьте на GitHub → Settings → SSH keys

┌──────────────────────────────────────────────────────────────┐
│ Проблема: Merge conflicts                                    │
└──────────────────────────────────────────────────────────────┘
  Решение:
  1. git status                    # Посмотреть конфликты
  2. Откройте файлы и разрешите конфликты
  3. git add .
  4. git commit -m "Resolve conflicts"

┌──────────────────────────────────────────────────────────────┐
│ Проблема: Случайно закоммитил .env файл                     │
└──────────────────────────────────────────────────────────────┘
  Решение:
  git rm --cached .env
  git commit -m "Remove .env from tracking"
  # Убедитесь что .env в .gitignore

┌──────────────────────────────────────────────────────────────┐
│ Проблема: Нужно отменить последний коммит                   │
└──────────────────────────────────────────────────────────────┘
  Решение:
  git reset --soft HEAD~1          # Сохранить изменения
  git reset --hard HEAD~1          # Удалить изменения (осторожно!)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📖 ДОПОЛНИТЕЛЬНЫЕ РЕСУРСЫ

Документация проекта:
  • README.md                   - Главная документация
  • GITHUB_SETUP.md            - Настройка GitHub
  • CONTRIBUTING.md            - Руководство для контрибьюторов
  • GIT_SETUP_COMPLETE.md      - Детальный отчет о setup

Скрипты:
  • ./setup-github.sh          - Интерактивная настройка GitHub

Внешние ресурсы:
  • https://git-scm.com/doc
  • https://github.com/git-guides
  • https://www.conventionalcommits.org

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✨ TIPS & TRICKS

• Проверяйте статус перед коммитом:
  git status

• Используйте осмысленные commit messages:
  ✅ "feat(ios): Add coffee order view"
  ❌ "fix stuff"

• Коммитьте часто, push реже:
  Делайте маленькие логические коммиты

• Всегда pull перед push:
  git pull origin main
  git push origin main

• Используйте ветки для всех изменений:
  Не работайте напрямую в main

• Регулярно синхронизируйтесь с main:
  git checkout feature-branch
  git merge main

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎯 NEXT STEPS

1. ☐ Создать репозиторий на GitHub
2. ☐ Запустить ./setup-github.sh
3. ☐ Push код на GitHub
4. ☐ Добавить команду (если есть)
5. ☐ Настроить branch protection
6. ☐ Создать develop ветку
7. ☐ Начать разработку!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Made with ☕ and ❤️

EOF
