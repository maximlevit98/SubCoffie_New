# =============================================
# Supabase Auth Provider Configuration
# =============================================
# Copy this file to .env and fill in the values
# DO NOT commit .env file to git

# Apple OAuth Configuration
# Get from: https://developer.apple.com/account/resources/identifiers/list/serviceId
# 1. Create a Service ID in Apple Developer Console
# 2. Enable "Sign in with Apple" capability
# 3. Configure redirect URLs to include: subscribecoffie://auth/callback
SUPABASE_AUTH_EXTERNAL_APPLE_CLIENT_ID=your.apple.service.id
SUPABASE_AUTH_EXTERNAL_APPLE_SECRET=your_apple_client_secret

# Google OAuth Configuration
# Get from: https://console.cloud.google.com/apis/credentials
# 1. Create OAuth 2.0 Client ID (iOS application)
# 2. Add authorized redirect URI: subscribecoffie://auth/callback
# 3. Download the JSON credentials
SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
SUPABASE_AUTH_EXTERNAL_GOOGLE_SECRET=your_google_client_secret

# SMS Provider Configuration (Optional - for production)
# Twilio Configuration for Phone Authentication
# Get from: https://console.twilio.com/
# Required only if you want to send real SMS in production
# For local development, test OTP codes are configured in config.toml
SUPABASE_AUTH_SMS_TWILIO_AUTH_TOKEN=your_twilio_auth_token

# Email SMTP Configuration (Optional - for production)
# For local development, emails are captured by Inbucket (http://127.0.0.1:54324)
# For production, configure a real SMTP provider like SendGrid
SENDGRID_API_KEY=your_sendgrid_api_key

# OpenAI API Key (Optional - for Supabase Studio AI features)
OPENAI_API_KEY=your_openai_api_key
